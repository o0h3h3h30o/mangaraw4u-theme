/**
 * Google OAuth Provider (Stub for demo)
 * Renders children without requiring Google OAuth credentials
 */

"use client";

export function GoogleOAuthProvider({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
